<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
  <div class="container">
    <%= link_to "🍚 きぶんご飯", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "🔑 ログイン", new_user_session_path, class: "btn btn-outline-light btn-sm me-2" %>
        </li>
        <li class="nav-item">
          <%= link_to "✨ 新規登録", new_user_registration_path, class: "btn btn-light btn-sm" %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<% unless user_signed_in? %>
  <!-- navbar直下の説明エリア -->
  <div class="bg-primary bg-opacity-10 border-bottom">
    <div class="container py-5 text-center">

      <!-- キャッチコピー -->
      <h5 class="mb-2">🍽 今の気分で、近くのお店を</h5>
      <p class="text-muted mb-4 small">
        現在地 × 気分で、あなたにぴったりのレストランを見つけます
      </p>

      <!-- 使い方（トップページの説明） -->
      <div class="row justify-content-center mb-4">
        <div class="col-md-3 col-6 mb-3">
          <div class="p-3 bg-white rounded shadow-sm h-100">
            <div class="fs-3 mb-2">📍</div>
            <p class="small mb-0">現在地を取得</p>
          </div>
        </div>

        <div class="col-md-3 col-6 mb-3">
          <div class="p-3 bg-white rounded shadow-sm h-100">
            <div class="fs-3 mb-2">😊</div>
            <p class="small mb-0">気分を選ぶ</p>
          </div>
        </div>

        <div class="col-md-3 col-6 mb-3">
          <div class="p-3 bg-white rounded shadow-sm h-100">
            <div class="fs-3 mb-2">🍽</div>
            <p class="small mb-0">お店をチェック</p>
          </div>
        </div>
      </div>

      <!-- CTA（ログイン必須） -->
      <%= link_to new_user_session_path,
          class: "btn btn-primary btn-sm px-4" do %>
        ログインして使ってみる
      <% end %>

      <p class="text-muted small mt-3 mb-0">
        ※ ご利用にはログインが必要です
      </p>

    </div>
  </div>
<% end %>
